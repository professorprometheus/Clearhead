import { AuthForm } from "@/components/AuthForm"
import { AuthShell } from "@/components/AuthShell"
export default function SignInPage(){return <AuthShell title="Welcome back." copy="Your projects and next steps are ready when you are."><AuthForm mode="sign-in"/></AuthShell>}
