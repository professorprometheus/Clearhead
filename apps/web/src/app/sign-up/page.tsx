import { AuthForm } from "@/components/AuthForm"
import { AuthShell } from "@/components/AuthShell"
export default function SignUpPage(){return <AuthShell title="Make room for what matters." copy="Start with every Pro feature for 30 days. No card, no pressure."><AuthForm mode="sign-up"/></AuthShell>}
