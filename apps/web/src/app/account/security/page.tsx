import { ShieldCheck } from "lucide-react"
import Link from "next/link"
import { requireSession } from "@/lib/session"
export default async function SecurityPage(){const session=await requireSession();return <div className="space-y-6"><div><p className="eyebrow">Security</p><h1 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">Your account is protected</h1></div><div className="web-card flex gap-4"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><ShieldCheck className="size-5"/></span><div><h2 className="font-semibold text-slate-950">{session.user.emailVerified?"Email verified":"Verify your email"}</h2><p className="mt-2 text-sm leading-6 text-slate-600">Resetting your password signs out other sessions. Google and password sign-in connect only when the verified email address matches.</p><Link href="/forgot-password" className="mt-4 inline-block text-sm font-semibold text-indigo-600">Change password securely →</Link></div></div></div>}
